<%= form_with(model: job_offer) do |form| %>
  <% if job_offer.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(job_offer.errors.count, "error") %> prohibited this job_offer from being saved:</h2>

      <ul>
        <% job_offer.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field mb-3">
    <%= form.label :job, "Puesto de trabajo", class: "form-label mb-2" %>
    <%= form.text_field :job, class: "form-control" %>
  </div>

  <div class="field mb-3">
    <%= form.label :description, "Descripción", class: "form-label mb-2" %>
    <%= form.text_area :description, class: "form-control" %>
  </div>

  <div class="mb-5">
    <%= form.submit "Publicar", class: "btn btn-primary" %>
  </div>
<% end %>
